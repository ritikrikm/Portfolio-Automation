const { chromium } = require('playwright');
const URL='https://iko.wd3.myworkdayjobs.com/IKO_Careers/job/Mississauga-ON/Graphic-Designer_REQ-12721';
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1700}});page.setDefaultTimeout(30000);await page.goto(URL,{waitUntil:'domcontentloaded',timeout:60000});const apply=page.locator('[data-automation-id="adventureButton"]');await apply.waitFor({state:'visible',timeout:20000});console.log('LIVE_JOB='+(await page.locator('[data-automation-id="jobPostingHeader"]').innerText()).trim());console.log('POSTED='+(await page.locator('[data-automation-id="postedOn"]').innerText()).replace(/\s+/g,' ').trim());await apply.click();await page.waitForTimeout(700);const manual=page.getByRole('button',{name:/apply manually/i});console.log('APPLY_OPTIONS='+JSON.stringify((await page.getByRole('button').allTextContents()).map(x=>x.trim()).filter(Boolean)));if(await manual.count())await manual.click();await page.waitForTimeout(1800);console.log('BODY='+(await page.locator('body').innerText()).replace(/\s+/g,' ').slice(0,10000));console.log('FIELDS='+JSON.stringify(await page.locator('input,textarea,select').evaluateAll(els=>els.map((e,i)=>({i,tag:e.tagName,id:e.id,name:e.name,type:e.type,ph:e.placeholder,aria:e.getAttribute('aria-label'),required:e.required,value:e.value||null})).filter(x=>x.id||x.name||x.ph||x.aria))));console.log('BUTTONS='+JSON.stringify((await page.getByRole('button').allTextContents()).map(x=>x.trim()).filter(Boolean)));await browser.close();})().catch(e=>{console.error(e.stack||e);process.exit(1)});